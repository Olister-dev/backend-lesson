//Modules - Encapsulate Code (only share minimum)
//ConomJS, every file is module (by default)

const name = require("./4-names");
const sayHi = require("./5-utils");
const data = require("./6-alternative");
require("./7-mind-granade");

// sayHi("susan");
// sayHi(name.john);
// sayHi(name.peter);
